---
import PageLayout from '@/layouts/PageLayout.astro';
import Icon from '@/components/ui/primitives/Icon/Icon.astro';
import Badge from '@/components/ui/data-display/Badge/Badge.astro';
import Card from '@/components/ui/data-display/Card/Card.astro';
import Button from '@/components/ui/form/Button/Button.astro';
import { Hero } from '@/components/hero';
import LetterGlitchBand from '@/components/patterns/LetterGlitchBand.astro';

const principles = [
  {
    icon: 'scan-search',
    title: 'Measure the boundary',
    body: 'Good security work starts where assumptions meet real systems: packet traces, certificates, logs, parsers, and the messy details between layers.',
  },
  {
    icon: 'sigma',
    title: 'Model the claim',
    body: 'When an argument matters, write down the state, invariants, threat model, and failure modes clearly enough that another person can attack them.',
  },
  {
    icon: 'git-branch',
    title: 'Ship inspectable artifacts',
    body: 'Research code should be readable, reproducible, and useful after the demo. The web presence should communicate that standard immediately.',
  },
];

const profileSignals = [
  'Systems security',
  'Cryptographic infrastructure',
  'Formal methods',
  'Post-quantum migration',
  'Protocol measurement',
  'Agentic evaluation',
];
---

<PageLayout
  title="Profile — Hypergalois"
  description="A research engineering profile focused on security infrastructure, formal methods, cryptography, and high-assurance systems."
  eagerReveal
>
  <Hero layout="centered" size="sm">
    <Badge slot="badge" variant="brand" pill>
      <Icon name="user-round" size="sm" />
      Profile
    </Badge>

    <h1 slot="title">Taste, rigor, and systems.</h1>

    <p slot="description">
      The profile page is not a CV dump. It is the argument for how the work is
      approached: careful enough for research, concrete enough for engineering.
    </p>
  </Hero>

  <section class="border-t border-border bg-background-secondary py-[var(--space-section-md)]">
    <div class="mx-auto grid max-w-6xl gap-10 px-6 lg:grid-cols-[1.05fr_0.95fr]">
      <div class="flex flex-col justify-center gap-5" data-reveal="right">
        <Badge variant="brand" pill class="self-start">Research posture</Badge>
        <h2 class="font-display text-3xl font-bold text-foreground md:text-4xl">
          Build the thing, test the claim, explain the boundary.
        </h2>
        <p class="text-lg leading-relaxed text-foreground-muted">
          The portfolio is shaped for top labs, PhD applications, and technical
          teams that read past the first screen. It should communicate depth without
          becoming heavy: strong artifacts, clear categories, readable notes, and
          enough visual energy to feel alive.
        </p>
        <p class="text-lg leading-relaxed text-foreground-muted">
          The through-line is research engineering: turning hard questions into
          artifacts that can be inspected, reproduced, challenged, and improved.
        </p>
      </div>

      <Card variant="elevated" padding="lg" class="h-full overflow-hidden" data-reveal="left" data-reveal-delay="1">
        <div class="mb-8 flex items-center justify-between gap-4">
          <div>
            <p class="text-xs font-bold uppercase tracking-wider text-foreground-muted">Profile signal</p>
            <h3 class="mt-1 font-display text-2xl font-bold text-foreground">Hypergalois</h3>
          </div>
          <div class="flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-brand-500/30 to-brand-500/5 text-brand-500">
            <Icon name="atom" size="lg" />
          </div>
        </div>
        <div class="grid gap-2">
          {profileSignals.map((signal) => (
            <div class="rounded-xl border border-border bg-background px-4 py-3 text-sm font-medium text-foreground">
              {signal}
            </div>
          ))}
        </div>
      </Card>
    </div>
  </section>

  <section class="border-t border-border bg-background py-[var(--space-section-md)]">
    <div class="mx-auto flex max-w-6xl flex-col gap-8 px-6">
      <div class="text-center" data-reveal>
        <Badge variant="brand" pill class="mb-5">Working principles</Badge>
        <h2 class="font-display text-3xl font-bold text-foreground md:text-4xl">
          How the work should feel.
        </h2>
      </div>

      <div class="grid gap-6 md:grid-cols-3">
        {principles.map((item, index) => (
          <Card hover data-reveal data-reveal-delay={index}>
            <div class="flex h-full flex-col gap-5">
              <div class="flex h-11 w-11 items-center justify-center rounded-xl bg-gradient-to-br from-brand-500/20 to-brand-500/5 text-brand-500">
                <Icon name={item.icon} size="md" />
              </div>
              <div>
                <h3 class="text-base font-semibold text-foreground">{item.title}</h3>
                <p class="mt-2 text-sm leading-relaxed text-foreground-muted">{item.body}</p>
              </div>
            </div>
          </Card>
        ))}
      </div>
    </div>
  </section>

  <section class="border-t border-border bg-background-secondary py-[var(--space-section-md)]">
    <div class="mx-auto grid max-w-6xl gap-8 px-6 lg:grid-cols-2">
      <Card variant="elevated" padding="lg" data-reveal="right">
        <Badge variant="brand" pill class="mb-6">For PhD committees</Badge>
        <h2 class="font-display text-3xl font-bold text-foreground">
          The site should make fit easy to evaluate.
        </h2>
        <p class="mt-4 text-base leading-relaxed text-foreground-muted">
          Clear research areas, technical writing, and artifact-led project pages
          reduce the burden on the reader. The design is polished, but the main
          job is signal: what problems matter, how they are attacked, and what
          evidence exists.
        </p>
      </Card>

      <Card variant="elevated" padding="lg" data-reveal="left" data-reveal-delay="1">
        <Badge variant="brand" pill class="mb-6">For labs and companies</Badge>
        <h2 class="font-display text-3xl font-bold text-foreground">
          Taste matters because the reader is technical.
        </h2>
        <p class="mt-4 text-base leading-relaxed text-foreground-muted">
          The site should feel engineered: fast, responsive, precise, and easy to
          scan. It should not over-explain itself, but it should reward someone
          who opens multiple pages and reads the details.
        </p>
      </Card>
    </div>
  </section>

  <section class="relative z-10 border-t border-border bg-background py-[var(--space-section-md)]">
    <div class="glitch:hidden mx-auto max-w-2xl px-6 text-center" data-reveal>
      <Badge variant="brand" pill class="mb-6">Next</Badge>
      <h2 class="mb-4 font-display text-4xl font-bold text-foreground text-balance">
        The artifacts carry the argument.
      </h2>
      <p class="mb-8 text-lg text-foreground-muted text-balance">
        Projects and writing are the center of the portfolio; this page sets the frame.
      </p>
      <Button size="lg" href="/projects" class="cta-btn-brand">
        Explore projects
        <Icon name="arrow-right" size="sm" />
      </Button>
    </div>

    <LetterGlitchBand>
      <h2 class="mb-4 font-display text-4xl font-bold text-foreground text-balance">
        The artifacts carry the argument.
      </h2>
      <p class="mb-8 text-lg text-foreground-muted text-balance">
        Projects and writing are the center of the portfolio; this page sets the frame.
      </p>
      <div class="flex flex-col justify-center gap-4 sm:flex-row">
        <Button size="lg" href="/projects" class="hero-btn-brand">
          Explore projects
          <Icon name="arrow-right" size="sm" />
        </Button>
        <Button size="lg" variant="outline" href="/blog">
          Read notes
          <Icon name="arrow-right" size="sm" />
        </Button>
      </div>
    </LetterGlitchBand>
  </section>
</PageLayout>
